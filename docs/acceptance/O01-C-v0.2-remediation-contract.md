# O01-C v0.2 — Remediation contract

## Purpose

This contract permits one and only one remediation of the immutable O01-C v0.1 failure.

```text
prior output      O01_QUALITY_COVERAGE_LAG_FAIL
failure class     VERSIONED_QUERY_CONTRACT_DEFECT
prior plan        sha256:7d323a9a920f1fe96832b5f6e631b4da257c3ce995ef433705aff95c3ed1643b
prior artifact    sha256:6a3d55557d16459e546839a6170db02e618b3a4c31cfadf6761a0d2885dab47f
v0.2 contract     sha256:f86672f2925343fccc61ebe0cb1085a470bbb54d062f1f936eed9347854ff3a3
```

## Sole permitted delta

```text
from
buyer-country IN ({country}) AND publication-date >= 20260701 AND publication-date <= 20260731 SORT BY publication-number ASC

to
buyer-country IN ({country}) AND publication-date >= 20260701 AND publication-date <= 20260731 SORT BY publication-number
```

TED rejected the SQL-style `ASC` token after `SORT BY publication-number`. No measurement window, stratum, country, language, notice type, sample allocation, page budget, retry budget, field projection, exclusion, metric, threshold, retention rule or authority boundary changes.

## Frozen execution

The campaign remains private and bounded. TED remains `CANDIDATE`; neither a successful campaign nor this remediation contract admits the source or authorises public claims.

The contract requires:

- 180 target notices across 12 country strata;
- six presentation languages;
- exactly two pages per country and at most 100 notices per page;
- the same quality and lag metrics and thresholds as v0.1;
- encrypted raw retention with no contact values;
- kill-switch and rollback evidence;
- zero fabricated evidence.

## Human authority

A new `LEGAL` and `PRIVACY_DATA_RIGHTS` decision is mandatory because the execution contract and manifest change. Earlier decisions do not survive this version transition.

```text
automatic human signature   false
automatic human approval    false
public launch                NO_GO
```
